package extendThread;

public class Main {

    public static void main(String[] args) {
        ExtendThread extendThread = new ExtendThread();
        extendThread.setCount(500);
        extendThread.start();
    }
}
